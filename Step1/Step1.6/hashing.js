



// 1.Division method
//  We will simply consider the modulo 10 of each element of the array(element % 10) 

// But if modulo 10 is same for two number we apply the linear chaining method---> 
// so if all modulo are same --> if all the elements go to the same hash index, we will call it a case of collision.


//2. Folding method

// 3. Mid-Square method